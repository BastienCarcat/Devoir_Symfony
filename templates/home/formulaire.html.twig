{% extends 'base.html.twig' %}

{% block title %}
    {{ h1 }}
{% endblock %}

{% block body %}
    <h1>{{ h1 }}</h1>
    {{ form(formulaire) }}
    <a class="btn btn-primary right" href="{{ path("home") }}" role="button">Retour</a>
{% endblock %}
